{
  "id": "claude-code-task",
  "type": "claude-code",
  "label": "Execute a Claude Code task",
  "description": "Run claude CLI with a prompt on the target repo",
  "config": {
    "prompt": "{{prompt}}",
    "allowedTools": ["Read", "Write", "Bash"]
  }
}
